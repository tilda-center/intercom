# intercom
Code for arduino to open the door

Requirements:
```bash
pip install ino
```

Building:
```bash
ino build
```

Uploading:
```bash
ino upload
```

The config file assumes that Arduino Nano 328 is used
